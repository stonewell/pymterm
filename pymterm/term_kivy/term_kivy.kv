<TermTextInput>:
    size_hint_y: None

<RootWidget>:
    txtBuffer: txtBuffer
    ScrollView:
        id: e_scroll
        TermTextInput:
            id: txtBuffer
            height: max(e_scroll.height, self.minimum_height)
